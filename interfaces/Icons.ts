export interface Icons {
  js?: boolean,
  bootstrap?: boolean,
  createReactApp?: boolean,
  gatsby?: boolean,
  github?: boolean,
  linkedin?: boolean,
  next?: boolean,
  react?: boolean,
  tailwind?: boolean,
  twitter?: boolean,
}